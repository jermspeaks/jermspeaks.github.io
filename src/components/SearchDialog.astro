---
import SearchDialogClientOnly from "./SearchDialogClientOnly.svelte";

type Props = {
  searchId?: string;
};

const { searchId = "search" } = Astro.props;
---

<SearchDialogClientOnly searchId={searchId} client:only="svelte" />
